# ExpressLRS Local RX Web UI

This is a local, RX-only Vite web UI for firmware built with `LOCAL_WEBUI=1`.

```sh
npm install
npm run dev
```

The default API endpoint is `http://10.0.0.1`. Override it with the connection field or with `?api=http://host`.
